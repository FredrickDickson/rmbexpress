import 'package:flutter/material.dart';

class BuyRmbScreen extends StatelessWidget {
  const BuyRmbScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Buy RMB'),
      ),
      body: const Center(
        child: Text('Buy RMB Screen - Coming Soon!'),
      ),
    );
  }
}